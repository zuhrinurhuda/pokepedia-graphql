/**
 * Generate random number
 * @param {number} max Max result number
 * @return {number} random number
 */
const getRandomInt = (max) => Math.floor(Math.random() * max);

export default getRandomInt;
